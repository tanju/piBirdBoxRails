<%= form_for(@tuersensor) do |f| %>
  <% if @tuersensor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tuersensor.errors.count, "error") %> prohibited this tuersensor from being saved:</h2>

      <ul>
      <% @tuersensor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movetype %><br />
    <%= f.number_field :movetype %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.datetime_select :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
